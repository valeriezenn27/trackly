import{Component,inject,OnInit}from'@angular/core';import{CommonModule}from'@angular/common';import{FormsModule}from'@angular/forms';import{RouterLink}from'@angular/router';import{ProductService}from'../services/product.service';import{Product}from'../models';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<div class="page-head"><div><p class="eyebrow">CATALOGUE</p><h1>Products</h1><p>Browse every product Trackly is watching.</p></div><span class="count">{{products.length}} products</span></div><section class="panel table-panel"><div class="toolbar"><label class="search">⌕ <input [(ngModel)]="search" (ngModelChange)="load()" placeholder="Search products by name"></label><select [(ngModel)]="category" (ngModelChange)="load()"><option value="">All categories</option><option *ngFor="let c of categories">{{c}}</option></select></div><div class="table-wrap"><table><thead><tr><th>Product</th><th>Price</th><th>Availability</th><th>Rating</th><th>Last scraped</th><th></th></tr></thead><tbody><tr *ngFor="let p of products"><td><div class="product-cell"><img [src]="p.imageUrl||'favicon.svg'"><div><b>{{p.name}}</b><small>{{p.category||'Uncategorised'}}</small></div></div></td><td><strong>{{p.currentPrice|currency:'GBP':'symbol':'1.2-2'}}</strong></td><td><span class="availability">{{p.availability||'Unknown'}}</span></td><td><span class="stars">★</span> {{p.rating||'—'}}</td><td>{{p.lastScrapedAt|date:'mediumDate'}}</td><td><a class="icon-button" [routerLink]="['/products',p.id]">→</a></td></tr></tbody></table></div><div class="empty" *ngIf="!products.length">No products found. Try running a scrape from Sources.</div></section>`})export class ProductsComponent implements OnInit{private service=inject(ProductService);products:Product[]=[];search='';category='';get categories(){return[...new Set(this.products.map(x=>x.category).filter(Boolean) as string[])]}ngOnInit(){this.load()}load(){this.service.list(this.search,this.category).subscribe({next:v=>this.products=v})}}
