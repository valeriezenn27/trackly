import{Component,inject,OnInit}from'@angular/core';import{CommonModule}from'@angular/common';import{FormsModule}from'@angular/forms';import{Source}from'../models';import{SourceService}from'../services/source.service';import{ScrapingService}from'../services/scraping.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<div class="page-head"><div><p class="eyebrow">COLLECTION</p><h1>Sources</h1><p>Choose where Trackly looks for product data.</p></div><button class="button" (click)="openForm()">＋ Add source</button></div><div class="notice" *ngIf="message">{{message}}</div><div class="source-grid"><article class="panel source-card" *ngFor="let s of sources"><div class="source-top"><div class="source-logo">B</div><span class="pill" [class.bad]="!s.isActive">{{s.isActive?'Active':'Inactive'}}</span></div><h2>{{s.name}}</h2><a [href]="s.baseUrl" target="_blank">{{s.baseUrl}}</a><div class="source-meta"><span>Type <b>{{s.sourceType}}</b></span><span>Added <b>{{s.createdAt|date:'mediumDate'}}</b></span></div><div class="card-actions"><button class="button" (click)="run(s)" [disabled]="busy===s.id">{{busy===s.id?'Scraping…':'Run scrape'}}</button><button class="ghost" (click)="openForm(s)">Edit</button></div></article></div><div class="modal-wrap" *ngIf="showForm"><form class="panel modal" (ngSubmit)="save()"><div class="panel-title"><h2>{{editing?'Edit':'Add'}} source</h2><button type="button" class="close" (click)="showForm=false">×</button></div><label>Name<input [(ngModel)]="form.name" name="name" required></label><label>Base URL<input [(ngModel)]="form.baseUrl" name="baseUrl" type="url" required></label><label>Source type<input [(ngModel)]="form.sourceType" name="sourceType"></label><label class="check"><input type="checkbox" [(ngModel)]="form.isActive" name="isActive"> Active</label><button class="button" type="submit">Save source</button></form></div>`})export class SourcesComponent implements OnInit{private service=inject(SourceService);private scrape=inject(ScrapingService);sources:Source[]=[];showForm=false;editing?:Source;busy='';message='';form:Partial<Source>={name:'',baseUrl:'https://',sourceType:'Product',isActive:true};ngOnInit(){this.load()}load(){this.service.list().subscribe(v=>this.sources=v)}openForm(s?:Source){this.editing=s;this.form=s?{...s}:{name:'',baseUrl:'https://',sourceType:'Product',isActive:true};this.showForm=true}save(){const request=this.editing?this.service.update(this.editing.id,this.form):this.service.create(this.form);request.subscribe(()=>{this.showForm=false;this.load()})}run(s:Source){this.busy=s.id;this.message='Scrape started. This may take a moment…';this.scrape.run(s.id).subscribe({next:j=>{this.busy='';this.message=`Scrape ${j.status.toLowerCase()}: ${j.productsFound} products found, ${j.productsUpdated} updated.`},error:()=>{this.busy='';this.message='The scrape could not be completed.'}})}}
