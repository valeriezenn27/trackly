import{Component,inject,OnInit}from'@angular/core';import{CommonModule}from'@angular/common';import{ActivatedRoute,RouterLink}from'@angular/router';import{ProductService}from'../services/product.service';import{PriceHistory,Product}from'../models';
@Component({standalone:true,imports:[CommonModule,RouterLink],template:`<a class="back" routerLink="/products">← Back to products</a><div *ngIf="product as p"><div class="detail-hero panel"><img [src]="p.imageUrl||'favicon.svg'"><div><p class="eyebrow">{{p.category||'PRODUCT'}}</p><h1>{{p.name}}</h1><div class="detail-price">{{p.currentPrice|currency:'GBP'}}</div><p><span class="availability">{{p.availability}}</span> <span class="stars">★★★★★</span> {{p.rating}}</p><a [href]="p.productUrl" target="_blank" rel="noopener">View original listing ↗</a></div></div><div class="detail-grid"><section class="panel"><div class="panel-title"><div><h2>Price history</h2><p>Every recorded price observation.</p></div></div><div class="chart" *ngIf="history.length"><svg viewBox="0 0 600 180" preserveAspectRatio="none"><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#7165f7" stop-opacity=".3"/><stop offset="1" stop-color="#7165f7" stop-opacity="0"/></linearGradient></defs><polyline [attr.points]="points" fill="none" stroke="#7165f7" stroke-width="4" vector-effect="non-scaling-stroke"/></svg></div><div class="empty" *ngIf="!history.length">No price history yet.</div></section><section class="panel compact"><h2>Product details</h2><dl><div><dt>Currency</dt><dd>{{p.currency}}</dd></div><div><dt>Last scraped</dt><dd>{{p.lastScrapedAt|date:'medium'}}</dd></div><div><dt>Created</dt><dd>{{p.createdAt|date:'mediumDate'}}</dd></div></dl></section></div><section class="panel table-panel"><table><thead><tr><th>Observed at</th><th>Price</th><th>Currency</th></tr></thead><tbody><tr *ngFor="let h of history"><td>{{h.scrapedAt|date:'medium'}}</td><td><strong>{{h.price|currency:'GBP'}}</strong></td><td>{{h.currency}}</td></tr></tbody></table></section></div>`})export class ProductDetailsComponent implements OnInit{private route=inject(ActivatedRoute);private service=inject(ProductService);product?:Product;history:PriceHistory[]=[];get points(){if(!this.history.length)return'';const values=this.history.map(x=>x.price),min=Math.min(...values),max=Math.max(...values),range=max-min||1;return values.map((v,i)=>`${i*600/Math.max(values.length-1,1)},${160-(v-min)*130/range}`).join(' ')}ngOnInit(){const id=this.route.snapshot.paramMap.get('id')!;this.service.get(id).subscribe(v=>this.product=v);this.service.history(id).subscribe(v=>this.history=v)}}
